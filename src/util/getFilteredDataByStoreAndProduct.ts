// import { useDataStore } from '../stores/Data'

// const dataStore = useDataStore()

type Props = {
  store_id: number
  product_id: number
}

export function getFilteredDataByStoreAndProduct({ store_id, product_id }: Props) {
  // const deliveriesData = dataStore.deliveries
  // const recommendationsData = dataStore.recommendations
  // const salesData = dataStore.sales
  // const arrayOfAllProductRecommended = recommendationsData.filter((recommendation) => {
  //   return recommendation.id_store == store_id && recommendation.id_product == product_id
  // })
  // const arrayOfAllProductDelivered = deliveriesData.filter((recommendation) => {
  //   recommendation.delivery_qty
  //   return recommendation.id_store == store_id && recommendation.id_product == product_id
  // })
  // const arrayOfAllProductSales = salesData.filter((recommendation) => {
  //   return recommendation.id_store == store_id && recommendation.id_product == product_id
  // })
  // return {
  //   arrayOfAllProductRecommended,
  //   arrayOfAllProductDelivered,
  //   arrayOfAllProductSales
  // }
}
